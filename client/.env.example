# If client & server are deployed on separate Vercel projects, set VITE_API_URL to your backend API URL (e.g. https://moh-server.vercel.app).
# If client & server are deployed together in a Vercel monorepo, leave VITE_API_URL empty (defaults to relative /api routes).
VITE_API_URL=https://vercel.com/kavishkasandaruwan608-9004s-projects/moh-server

